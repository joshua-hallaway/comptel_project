# System & Environment
- Downloaded and installed **Ubuntu (WSL)** on Windows
- Installed **Miniconda** in WSL
- Created project conda environment
	- **Name:**`dSph-dark-matter`
	- **Python Version:** 3.10.20
- Activated environment and verified interpreter path

# GammaLib Installation
- Downloaded **GammaLib** source
- Built **GammaLib** from source
- Installed **GammaLib 2.1.0.dev** to conda environment
- Installed and verified **GammaLib Python wrappers**

# ctools Installation
- Downloaded **ctools** source
- Built **ctools** from source
- Installed **ctools 2.1.0.dev** to conda environment
- Installed and verified **ctools Python wrappers**

# Workspace Setup
- Created project directory
	- `~/comptel_project`
- Initialized Git repository inside directory
- Created branches
	- `main`
	- `joshua_test_branch`
- Pushed `main` branch to GitHub
- Added `.gitignore`

# Wrapper Testing
- Created
	- `test_wrappers.py`
	- `run_test.sh`
- Verified GammaLib and ctools load correctly
- Committed test scripts to `joshua_test_branch`
- Pushed `joshua_test_branch` to GitHub
- Merged `main` and `joshua_test_branch`
- Renamed `joshua_test_branch` to `dev`

# Linux Workflow
- Created global aliases
	- `start`
		- `conda activate dSph-dark-matter`
	- `stop`
		- `conda deactivate`
- Created local alias within conda environment
	- `versions`
		- `python -c "import gammalib; print(f'GammaLib {gammalib.__version__}')" && python -c "import ctools; print(f'ctools {ctools.__version__}')"`

# Next Steps
- Meet with Advisor about what's next